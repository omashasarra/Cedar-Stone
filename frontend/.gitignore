# Environment variables (keeps keys private)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Dependencies (don't upload node_modules)
/node_modules

# Production build output
/build
/dist

# Logs and system files
npm-debug.log*
yarn-debug.log*
.DS_Store